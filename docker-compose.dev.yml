services:
  web:
    build:
      context: ./src
      dockerfile: ../.docker/Dockerfile
      target: dev
    ports:
      - "5173:5173"
    volumes:
      - ./src:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
